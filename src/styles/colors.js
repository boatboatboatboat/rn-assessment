import {DarkTheme, DefaultTheme} from "@react-navigation/native";

export const DarkColors = {
    background: DarkTheme.colors.card,
    foreground: DarkTheme.colors.text,
    border: DarkTheme.colors.border,
    listBackground: DarkTheme.colors.background,
};

export const DefaultColors = {
    background: DefaultTheme.colors.card,
    foreground: DefaultTheme.colors.text,
    border: DefaultTheme.colors.border,
    listBackground: DefaultTheme.colors.background,
};
